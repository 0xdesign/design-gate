# Changelog

## 0.1.0 (unreleased)

- First release: `init`, `check`, `report`, `explain`, `waive`, `open`, `runs`, `login`, `status`.
- Deterministic facts compiler and gates (contrast, targets, focus, axe, heading outline, cross-screen drift).
- Jev judge over a 38-question bank with designer-written fixes; fake judge for tests.
- Agent-as-observer flow (no second API key); optional Anthropic API observer.
- Run page per design turn with screenshots, scores, fixes, before/after slider and per-decision provenance.
- Agent Skill (agentskills.io format) installed into detected agents by `init`.
